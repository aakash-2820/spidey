export const REQUIRED_FIELDS=['destination','durationDays','budget','travellers','interests'];
export function detectMissingTripInformation(data){return REQUIRED_FIELDS.filter(key=>key==='interests'?!Array.isArray(data.interests)||data.interests.length===0:data[key]===null||data[key]===undefined||data[key]==='')}
export function nextClarification(data){const field=detectMissingTripInformation(data)[0];const questions={destination:'Where are you travelling?',durationDays:'How many days are you planning?',budget:'What budget should I plan around?',travellers:'How many people are travelling?',interests:'What kind of places are you interested in?'};return field?{field,question:questions[field]}:null}
