import { resolveDestination } from './destination.service.js';
const headers={'user-agent':'TravelMind/1.0 (India travel planner)'};
export async function searchPlaces(query,city){const destination=await resolveDestination(city);const url=new URL('https://nominatim.openstreetmap.org/search');url.searchParams.set('q',`${query}, ${destination.displayName}`);url.searchParams.set('format','jsonv2');url.searchParams.set('limit','8');url.searchParams.set('countrycodes','in');url.searchParams.set('addressdetails','1');url.searchParams.set('viewbox',`${destination.boundingBox[2]},${destination.boundingBox[1]},${destination.boundingBox[3]},${destination.boundingBox[0]}`);url.searchParams.set('bounded','1');const r=await fetch(url,{headers,signal:AbortSignal.timeout(10_000)});if(!r.ok)throw Object.assign(new Error('Place search provider unavailable'),{status:502});return(await r.json()).map(x=>({id:String(x.place_id),name:x.name||query,displayName:x.display_name,latitude:Number(x.lat),longitude:Number(x.lon),type:x.type,address:x.address,source:'NOMINATIM',destination}))}
