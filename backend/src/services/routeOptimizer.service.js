const rad=x=>x*Math.PI/180;export const distance=(a,b)=>{const R=6371,dLat=rad(b.latitude-a.latitude),dLon=rad(b.longitude-a.longitude);const q=Math.sin(dLat/2)**2+Math.cos(rad(a.latitude))*Math.cos(rad(b.latitude))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(q))};
export function optimizeRoute(places,start={latitude:13.0604,longitude:80.2496}){const left=[...places],ordered=[];let current=start,total=0;while(left.length){left.sort((a,b)=>distance(current,a)-distance(current,b));const next=left.shift();total+=distance(current,next);ordered.push(next);current=next}return{items:ordered,totalDistanceKm:+total.toFixed(1),estimatedTravelMinutes:Math.round(total/22*60)}}
