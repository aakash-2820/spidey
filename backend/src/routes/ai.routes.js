import { Router } from 'express';import { parseNaturalLanguage } from '../services/ai.service.js';import { detectCommand } from '../services/nlp.service.js';const router=Router();router.post('/parse-trip',async(req,res,next)=>{try{res.json(await parseNaturalLanguage(req.body.request))}catch(e){next(e)}});router.post('/chat',(req,res)=>{const command=detectCommand(req.body.message);res.json({command,reply:command.intent==='QUESTION'?'I can explain recommendations or update your trip.':`I understood this as ${command.intent}. Confirm to apply it.`})});export default router;
